import type { Metadata } from "next";
import type { ReactNode } from "react";
export const metadata:Metadata={title:"CBAM Bulk CSV Calculator | Official CN & Country Data",description:"Calculate up to 200 EU CBAM import rows using country, CN/TARIC, reporting year, production year and official EU reference data.",alternates:{canonical:"https://greenengineeringtools.com/cbam-calculator/bulk"}};
export default function Layout({children}:{children:ReactNode}){return children}
